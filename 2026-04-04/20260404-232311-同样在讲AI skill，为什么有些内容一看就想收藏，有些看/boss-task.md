@ALL
归档路径建议：2026-04-05 / 20260404-232311+AI工作流别再只堆工具和模型用户真正想收藏的是可直接照着跑的整套路径 / material files

# Boss Cycle 20260404-232311

## 1. 本轮已确认前提
- 已读取 `Boss/CLAUDE.md` 与 `.role-sessions/current_task.md`。
- 已执行 `git branch --show-current`，当前分支为 `RedbookClaw`，满足继续工作的前提。
- 已检索 `SAF-TaskAssignAndProgress.md`，当前仓库未找到该文件，按缺失状态如实记录，不额外引用不存在文件。
- 本轮 Boss 只写一个任务文件：`Boss/BOSS-CYCLE-20260404-232311.md`。
- 最终产物仅用于预览、复审与手动发布，不做公开自动发布。

## 2. 统一归档包
- `date`：`2026-04-05`
- `timestamp`：`20260404-232311`
- `archive_title`：`AI工作流别再只堆工具和模型用户真正想收藏的是可直接照着跑的整套路径`
- `archive_package`：`2026-04-05 / 20260404-232311+AI工作流别再只堆工具和模型用户真正想收藏的是可直接照着跑的整套路径 / material files`
- `publish_boundary`：`仅用于预览、复审与手动发布，不自动公开发布`

## 3. 趋势来源与证据边界
- 优先入口固定为：`https://www.newrank.cn/search/trend/skill`。
- 本轮已按要求优先尝试读取 Newrank 趋势页。
- 当前会话内未成功取得该页面的可复核 detail、热度值、排名或爆文明细。
- 按任务要求，Newrank detail 不完整时应使用 `yhslgg-arch/url-reader` 作为 required fallback。
- 但当前会话可用 skills 仅见 `simplify` 与 `claude-api`，未提供 `yhslgg-arch/url-reader`，因此本轮无法实际执行该 fallback。
- 因此本轮所有下游产物都必须统一遵守以下边界：
  1. 只能写“已优先检查 Newrank 趋势入口”；
  2. 只能写“本轮未成功取得 `search/trend/skill` 的可复核 detail”；
  3. 只能写“指定 required fallback skill 当前环境不可执行”；
  4. 不得编造任何趋势详情、排名、热度、截图结论、爆文链接或榜单摘要；
  5. 不得把 fallback 写成已完成。

## 4. 本轮主题方向
### 主选题
- `AI工作流别再只堆工具和模型，用户真正想收藏的是可直接照着跑的整套路径`

### 备选题
1. `同样都在讲 AI skill，为什么真正被收藏的是能直接执行的 workflow`
2. `不是工具不够多，是你的 AI workflow 还没接到结果上`
3. `别再只盘点模型和功能了，用户更想拿走的是一条能复用的产出路径`
4. `从 AI skill 到 workflow skill，真正拉开差距的是流程能不能稳定跑完`
5. `会装工具不等于会交付，真正有价值的是一套可复审可复用的 AI 工作流`

## 5. 人群、问题、结果口径
- 目标人群：关注 `openclaw`、`openclaw skill`、`AI skill`、`workflow skill`、`copilot skill` 的内容创作者、内容运营、独立开发者与小团队负责人。
- 核心问题：很多 AI 内容仍在堆工具名、模型名、提示词名词，但读者真正卡住的是“具体怎么接、怎么跑、怎么复用、怎么验收”。
- 本轮结果目标：给下游角色一套可预览、可复审、可手动发布的中文内容包，重点强调——`真正更容易被收藏、转发、评论和后续照着做的，不是工具清单，而是能直接接进产出的工作流。`

## 6. 关键词检索边界
- Topic 必须优先围绕这些关键词组织检索与表达：`openclaw`、`openclaw skill`、`AI skill`、`workflow skill`、`copilot skill`。
- 这些关键词当前只能作为“优先检索方向”和“内容聚焦边界”，不能被写成“Newrank 已确认爆发”的趋势结论。
- 如果下游没有新增可核验证据，只能把这些词写成主题聚焦对象，不能写成榜单事实。

## 7. 趋势判断与传播角度
- 趋势判断只能停留在可验证边界：当前仅能确认“任务要求优先参考 Newrank 趋势页”，但本轮未成功获取该页 detail。
- 因此本轮内容不能写成“新榜显示某关键词爆了”，只能写成：围绕 AI skill / workflow / 可复用路径的表达更适合形成收藏与复用型内容，但来源证据边界必须如实说明。
- 最适合放大的传播反差：`内容里讲了很多 AI 名词` 与 `读者依然不知道下一步怎么真正做出结果`。
- 最值得收藏的认知点：`读者真正想带走的，不是模型名和工具名，而是这套工作流到底该如何接进真实产出。`
- 本轮表达重点应从“模型更强了没有”转向“这套路径能不能让我今天直接照着跑”。

## 8. Topic 交付要求
- Topic 开头必须明确写出：`https://www.newrank.cn/search/trend/skill` 是优先入口，但本轮未成功取得可复核 trend detail。
- Topic 必须明确写出：任务要求中的 `yhslgg-arch/url-reader` required fallback 当前环境不可执行。
- Topic 只允许使用当前任务已确认的来源边界，不得扩写任何未经核验的新榜细节。
- Topic 必须产出可直接给 Draft 使用的中文 topic package，至少包含：
  - 主选题
  - 3-5 个备选题
  - 目标人群
  - 问题定义
  - 表达边界
  - 趋势判断
  - viral references
  - rewrite angle
  - hook breakdown
  - `Viral Copy Breakdown`
  - `Rewrite Direction`
- Topic 必须显式写出：本轮归档时间戳统一为 `20260404-232311`，不得沿用旧时间戳。
- Topic 的 `Viral Copy Breakdown` 至少拆清：hook、结构、冲突点、承诺、证据、CTA，只能拆传播模式，不能复制外部原句。
- Topic 要优先把方向收口到：`为什么用户更愿意保存可直接复用的工作流内容，而不是只看模型、工具或提示词本身。`

## 9. Draft 交付要求
- Draft 必须输出：
  - 1 个最终中文标题
  - 2 个备选标题
  - 1 篇中文正文
  - 1 个标签区块
  - 1 个独立的 `Viral Copy Breakdown` 小节
- Draft 必须重写爆款结构，不得复制外部原句、标题句、摘要句。
- Draft 正文建议结构：
  1. 开头先做认知反转：大家以为用户在追更强的模型、更全的工具和更神的 prompt，但真正更想收藏的是能直接照着跑的 workflow；
  2. 再做痛点代入：为什么看了很多 AI 内容，实际还是不知道下一步怎么做；
  3. 中段拆 3-4 个断点：只讲工具名、只讲功能点、只讲一句 prompt、没有角色分工与复审节点；
  4. 给出轻量解法：固定输入模板 + 角色拆分 + 中间复核 + 最终归档；
  5. 结尾做收藏/评论型 CTA。
- Draft 正文语气优先“踩坑后复盘、方法升级”的口吻，不写成工具测评、资讯搬运或安装教程。
- Draft 必须如实继承来源限制：不得出现未验证的榜单、热度、排名、爆文链接、截图结论。
- Draft 必须明确说明这是对爆款模式的改写，不是原句复刻。
- Draft 的归档字段必须统一到 `20260404-232311`。

## 10. Cover 交付要求
- 封面必须围绕主选题，不做榜单截图风，不做工具测评风，不做自动发布暗示。
- 可用封面文案方向：
  - `AI 工作流别再只堆工具和模型了`
  - `真正让人收藏的是可直接照着跑的路径`
  - `别只记功能名，先把流程接起来`
- 视觉重点：流程断点、协作链路、稳定产出、复审节点。
- 禁止元素：榜单截图、虚构热度、夸张收益承诺、公开发布暗示。
- 至少给出 3 个 cover candidates，并明确主推版本。
- Cover 的归档字段必须统一到 `20260404-232311`。

## 11. Review 复审口径
- 必须检查是否明确写出来源边界：
  - Newrank 优先；
  - 本轮未成功取得 `search/trend/skill` 的可复核 detail；
  - `yhslgg-arch/url-reader` 当前环境不可执行。
- 必须检查是否出现未验证的数据、排名、热度、爆文链接、详情页摘要或截图结论。
- 必须检查 Draft 是否做到“结构改写，不是句子复制”。
- 必须检查 Draft 是否真正覆盖“谁负责、怎么接、如何复用、如何校验”。
- 必须检查 `Viral Copy Breakdown` 是否写清楚 hook、结构、冲突点、承诺、证据、CTA。
- 必须检查 Topic / Draft / Cover / Review 的归档字段是否全部统一到 `20260404-232311`。
- 必须检查最终产物仅用于预览、复审与手动发布，不自动公开发布。

## 12. Viral Copy Breakdown
- **hook**：先抛出反差——AI skill、workflow、copilot 相关内容越来越多，但很多内容看完还是无法直接产出结果。
- **结构**：认知反转 → 代入痛点 → 拆解流程断点 → 给出可复用路径 → 收藏/评论 CTA。
- **冲突点**：创作者还在堆模型名和工具名，读者已经更在意“我能不能今天就拿去跑”。
- **承诺**：读者看完能快速判断，为什么有些 AI 内容只提供信息感，而另一些内容更容易被收藏和真正执行。
- **证据**：只允许使用当前已核验的来源状态、任务约束与流程复盘判断；不得伪造 Newrank detail 或 fallback 输出。
- **CTA**：引导读者收藏这套路径型表达框架，或评论自己最常卡住的 workflow 节点。

## 13. Rewrite Direction
- 保留的传播模式：反常识开头、痛点复盘、断点拆解、给路径模板、结尾互动。
- 必须重写的部分：标题句式、段落推进、案例顺序、结尾表达与 CTA。
- 绝不能复制的内容：任何外部原句、爆文标题、未验证榜单结论、热度数字、截图信息、详情页摘要。
- 重写方向：把“又出了什么新工具/新模型”改写成“为什么越来越多用户只愿意保存真正能接进结果的 workflow”；把“功能展示”改成“可直接复用的产出路径”。

## 14. 风险边界
- 不碰 `master/main`。
- 不修改 `node5`。
- 不编造 Newrank 详情、fallback 输出、热度数据、排名或爆文证据。
- 不直接复制外部标题句、摘要句、爆款原句。
- 不做公开发布。

## 15. 本轮交付说明
- 本轮 Boss 只写这一个 task file：`Boss/BOSS-CYCLE-20260404-232311.md`。
- 下游角色按本文件统一口径继续产出 Topic / Draft / Cover / Review。
- 本文件已满足 archive-ready 交付要求，可直接归入：`2026-04-05 / 20260404-232311+AI工作流别再只堆工具和模型用户真正想收藏的是可直接照着跑的整套路径 / material files`。
- 本轮未执行任何公开发布动作。
