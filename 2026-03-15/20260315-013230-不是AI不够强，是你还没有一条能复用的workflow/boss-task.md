@ALL
归档路径建议：2026-03-15 / 20260315-013230+AI技能别再堆提示词先把稳定交付的流程接起来 / material files

本轮 Boss 任务包

1. 人群与目标
- 目标人群：想把 AI 真正接入稳定产出的个人创作者、内容运营、独立开发者。
- 核心问题：为什么很多人已经接了 AI skill / Copilot / workflow，结果还是忽高忽低、很难复用。
- 本轮目标：输出一套可预览、可复审、可手动发布的小红书内容包，不公开自动发布。

2. 主题要求
- 主题优先级：先用新榜趋势页 https://www.newrank.cn/search/trend/skill 做方向判断。
- 当前可验证限制：已按要求读取当前任务与历史 Boss 留痕，但本轮会话内没有拿到新的 Newrank 可核验详情。
- 当前来源边界：若 Newrank detail 不可用，规则要求使用 `yhslgg-arch/url-reader` 作为 fallback；但当前环境仅提供 `simplify` 与 `claude-api` skill，未提供该 skill，因此不能伪造 fallback 已执行成功。
- 因此本轮不得编造新榜榜单名次、热度值、代表作品、爆文链接或任何未验证趋势细节。
- 主选题建议：AI 技能别再堆提示词，真正开始拉开差距的是你能不能把稳定交付的流程接起来。
- 必须产出 viral-copy breakdown，拆清 hook、结构、冲突点、承诺、证据、CTA，但所有趋势表述都只能基于已验证限制与已留痕方向。

3. 给 Topic 的明确口径
- 必须先把 `https://www.newrank.cn/search/trend/skill` 写成首要趋势入口。
- 必须优先围绕 `openclaw`、`openclaw skill`、`AI skill`、`workflow skill`、`Copilot skill` 组织检索方向，并写清哪些只有邻近信号、哪些仍无可核验详情。
- 若 Newrank 详情仍不可用，必须明确写出：入口已优先检查，但详情受限/缺失；当前会话要求 fallback 使用 `yhslgg-arch/url-reader`，但该 skill 当前不可用，因此不能把 fallback 结果写成已完成。
- Topic 必须输出可直接交给 Draft 的中文 topic package，至少包含：主选题、3-5 个备选题、目标人群、趋势线索、viral references、rewrite angle、hook breakdown。
- 必须包含单独 `Viral Copy Breakdown` 段落，至少拆出：hook、结构、冲突点、承诺、证据、CTA。
- 必须包含单独 `Rewrite Direction` 段落，明确哪些模式保留、哪些表达改写、哪些句子不能复制。
- Topic 方向要收口到：用户在意的已经不只是 AI 会不会回一句漂亮答案，而是它能不能把一整条流程稳定跑完并留下可复用结果。
- 归档信息必须统一为：
  - date：`2026-03-15`
  - timestamp：`20260315-013230`
  - archive_title：`AI技能别再堆提示词先把稳定交付的流程接起来`
  - archive_package：`2026-03-15 / 20260315-013230+AI技能别再堆提示词先把稳定交付的流程接起来 / material files`

4. 给 Draft 的明确交付口径
- 必写中文标题、中文正文、标签。
- 必写一个单独小节：`Viral Copy Breakdown` 或等价区块。
- 必须重写爆款结构，不得照搬原句。
- 标题优先方向：
  - AI技能别再堆提示词，先把稳定交付的流程接起来
  - 不是 AI 不够强，是你还没有一条能复用的 workflow
  - 真正拉开差距的，不是 prompt，而是能不能把流程跑完
- 正文建议框架：
  1) 先做认知反转：不是 AI 不行，是流程没接起来；
  2) 再给痛点代入：为什么同样的 AI skill，结果总是忽高忽低；
  3) 中段拆 3 个流程断点：输入混乱、过程不可复用、结果没人校验；
  4) 给一套轻量解法：固定输入模板 + 角色拆分 + 输出复核；
  5) 结尾做收藏/评论型 CTA。
- 正文语气：优先使用“踩坑后复盘”口吻，不写成工具测评，也不写成空泛方法论。
- 表达边界：只能引用 Topic 中已写明的来源限制，不得补写任何不可见趋势细节。
- 归档目录、时间戳、中文标题必须与本轮统一 package 对齐。

5. 给 Cover 的明确方向
- 封面标题方向：强认知反差，不做工具测评风。
- 可用封面文案方向：
  - 别再堆提示词，先把流程接起来
  - 不是 AI 不够强，是 workflow 太散
  - 真正拉开差距的是稳定交付
- 视觉重点：流程断点、协作链路、稳定交付、可复用结果。
- 避免元素：榜单截图、虚构热度、夸张收益承诺、自动发布暗示。
- 归档目录、时间戳、中文标题必须与本轮统一 package 对齐。

6. 给 Review 的复审口径
- 检查是否明确写出来源边界：新榜优先、详情未取到时按要求应走 `yhslgg-arch/url-reader`，但当前环境 skill 不可用，因此 fallback 未执行成功。
- 检查是否出现未验证的数据、排名、热度、爆文链接。
- 检查是否把关键词命中状态写清：`openclaw` / `openclaw skill` / `AI skill` / `workflow skill` / `Copilot skill` 哪些只有邻近线索，哪些证据不足。
- 检查标题与正文是否只是借鉴结构，而非复制句子。
- 检查正文是否真正覆盖“谁负责、怎么接、如何复用、如何校验”。
- 检查 `Viral Copy Breakdown` 是否写清 hook、结构、冲突点、承诺、证据、CTA。
- 检查输出是否支持归档：可直接落到 `date / timestamp+Chinese title / material files`。
- 检查最终产物仅用于预览、复审与手动发布，不自动公开发布。

7. 风险边界
- 不碰 `master/main`。
- 不做公开发布。
- 不编造趋势证据。
- 不把 fallback 当成已经成功执行。
- 不把工具缺失或抓取受限包装成已完成抓取。
- 不让 Topic / Draft / Cover / Review 使用不同时间戳，避免归档包漂移。

8. 交付说明
- 本轮 Boss 只写一个 boss task file。
- 下游角色以本文件与 `.role-sessions/current_task.md`、`@TOPIC` 当前方向为准继续收口。
- 统一归档包：`2026-03-15 / 20260315-013230+AI技能别再堆提示词先把稳定交付的流程接起来 / material files`。
