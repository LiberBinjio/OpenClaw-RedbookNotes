@ALL
归档路径建议：2026-04-05 / 20260405-174656+AI技能开始卷稳定交付链了真正拉开差距的是谁能把Skill接进可复用workflow / material files

本轮 Boss 任务包

1. 人群与目标
- 目标人群：正在把 AI 接进内容生产、知识处理、协作交付的创作者、运营、独立开发者和小团队负责人。
- 核心问题：同样都在看 AI skill、OpenClaw、workflow，为什么有人已经能稳定产出，有人还是每轮重开、结果飘忽。
- 本轮目标：输出一套可预览、可复审、可手动发布的小红书内容包，不做自动公开发布。
- 归档要求：所有下游产物按 `2026-04-05 / 20260405-174656+AI技能开始卷稳定交付链了真正拉开差距的是谁能把Skill接进可复用workflow / material files` 对齐命名与整理。

2. 主题要求
- 主题优先级：先看 Newrank 趋势入口 `https://www.newrank.cn/search/trend/skill`。
- 本轮检索关键词边界：`openclaw`、`openclaw skill`、`AI skill`、`workflow skill`、`copilot skill`。
- 当前可安全收口的趋势判断：公开可见线索正在把讨论重点从“有没有更多 skill”推向“能不能把这些 skill 接成一条稳定、可复核、可归档、可复用的 workflow”。
- 主选题：AI 技能开始卷稳定交付链了，真正拉开差距的是谁能把 Skill 接进可复用 workflow。
- 必须产出 `Viral Copy Breakdown`，拆清 hook、结构、冲突点、承诺、证据类型与 CTA；只能重写结构，不得复制原句。

3. 来源边界与检索状态
- 已按要求优先检查 `https://www.newrank.cn/search/trend/skill`。
- 本轮对该入口的直接抓取仍未取得可复核 detail：既有留痕显示 `WebFetch` 返回 `API Error 400`，因此不能把该入口写成已成功拿到 trend 明细。
- 按任务要求，Newrank detail 不可用时应使用 `yhslgg-arch/url-reader` 作为 required fallback；但当前会话仅提供 `simplify` 与 `claude-api` 两个 skill，未提供 `yhslgg-arch/url-reader`，因此不能写成已执行 fallback。
- 因此本轮只能引用公开可见的 Newrank 邻近标题级线索与既有稳定口径；不得补写热度值、排名、互动量、榜单字段、截图结论或未验证详情页正文。
- 当前可引用的公开线索包括：
  - 《百度电商Skill登陆OpenClaw，五大能力全开放》：说明 Skill 已从抽象能力走向具体任务落地，并直接与 OpenClaw 生态绑定。
  - 《腾讯再推“养虾”新措施，上线“中国专供”SkillHub》：说明中文技能生态与平台化分发叙事仍在升温。
  - 《你和最会玩“龙虾”的人，可能只差这8个硬核Skills》：说明传播重点正在从“有没有工具”转向“能力怎么真正用起来”。
  - 《腾讯ima宣布上线 ima skills》：说明 skills 正在继续向知识库、笔记、内容处理场景扩散。
  - `data.newrank.cn` 2026-03-31 行业更新摘要提到 OpenClaw 与标准化 CLI / 自动化 Skills：说明生态传播语言继续向标准化、可接入、可复用能力倾斜。
- 关键词命中状态：
  - `openclaw`：证据最稳。
  - `openclaw skill`：有稳定邻近文章线索，但仍非 `search/trend/skill` 直连详情。
  - `AI skill`：主要依赖 skills 上线、平台化分发、应用生成等邻近信号。
  - `workflow skill`：主要依赖标准化 CLI、自动化 skills、多人协作、链路执行等邻近信号。
  - `copilot skill`：本轮没有足够强的直连证据，不建议作为主叙事。

4. 给 Topic 的明确交付口径
- 必须先把 Newrank 入口写进来源状态，再写清：本轮入口已优先检查，但未成功取得可复核 detail。
- 必须明确写出：按要求应使用 `yhslgg-arch/url-reader` fallback，但当前环境 skill 不可用，未能执行，不得伪造抓取结果。
- Topic 必须输出一个可直接交给 Draft 的中文 topic package，至少包含：主选题、3-5 个备选题、目标人群、趋势线索、viral references、rewrite angle、hook breakdown。
- 必须单列 `Viral Copy Breakdown`：至少拆出 hook、结构、冲突点、承诺、证据、CTA。
- 必须单列 `Rewrite Direction`：明确哪些模式保留、哪些表达改写、哪些句子绝不能复制。
- 必须把 `date`、`timestamp`、`archive_title`、`archive_package` 全量更新到本轮：`2026-04-05`、`20260405-174656` 与统一中文标题。
- 趋势表达要收口到：竞争点正在从“会不会调 Skill”升级为“能不能把多个节点接成稳定 workflow 并反复复用”。

5. 给 Draft 的明确交付口径
- 必须继承 Topic 中真实可验证的来源状态，尤其是 Newrank-first、detail 未取到、fallback 不可用、当前只有邻近趋势证据这些限制。
- 必须输出：1 个最终中文标题、2 个备选标题、1 篇中文正文、1 个标签区块。
- 必须附带简短 `Viral Copy Breakdown` 区块。
- Draft 必须重写爆款结构，不得照搬新闻标题、摘要、导语或 Topic 原句。
- 标题优先方向：
  - AI 技能开始卷稳定交付链了，真正拉开差距的是谁能把 Skill 接进可复用 workflow
  - 同样都在接 AI skill，为什么有人已经做成稳定产线，有人还是每轮返工
  - 不是 Skill 不够多，是你还没把它们接成一条可复审的 workflow
- 正文建议框架：
  1) 先做认知反转：问题不在 skill 数量，而在 workflow 没接通；
  2) 再给痛点代入：为什么同样在用 AI，结果却忽高忽低；
  3) 中段拆 4 个流程断点：输入不稳定、角色不清、结果不复核、材料不归档；
  4) 给一套轻量解法：固定输入模板 + Skill 分工 + 节点复核 + archive 命名；
  5) 结尾做收藏 / 评论 / 复盘型 CTA。
- 正文语气优先使用“踩坑后复盘”口吻，不写成工具测评，也不写成空泛方法论。
- 必须保留事实说明：`Newrank 搜索入口已优先检查，但本轮未成功取得可复核 detail；按要求应使用 yhslgg-arch/url-reader 回退，但当前环境 skill 不可用，未能执行。`
- 不得公开发布，不得写成自动发布指令。

6. 给 Cover 的明确方向
- 封面方向必须与统一中文标题一致，并支持归档目录下独立留存。
- 主标题建议突出：`AI技能开始卷稳定交付链了`。
- 副标题建议突出：`真正拉开差距的是谁能把Skill接进可复用workflow`。
- 可用封面文案方向：
  - 不是 AI 不够强，是你的 workflow 太散
  - 同样在接 Skill，为什么别人越做越稳
  - 别急着加工具，先把整条流程接起来
- 视觉重点：流程断点、协作链路、稳定交付。
- 避免元素：榜单截图、虚构热度、夸张收益承诺。
- 封面归档时间戳与中文标题必须与本轮 package 完全一致。

7. 给 Review 的复审口径
- 检查是否明确写出来源边界：Newrank 优先、`search/trend/skill` 详情未核验、`yhslgg-arch/url-reader` 当前环境不可调用，因此没有新增可核验详情页证据。
- 检查是否出现未验证的数据、排名、热度、爆文链接正文。
- 检查 Topic 是否写清：`openclaw` 与 `openclaw skill` 当前有稳定邻近线索，但 `search/trend/skill` 直连详情仍缺失；`AI skill`、`workflow skill` 主要靠邻近趋势证据支撑；`copilot skill` 证据最弱。
- 检查 Topic / Draft 是否补齐 `Viral Copy Breakdown` 与 `Rewrite Direction`。
- 检查标题与正文是否只是借鉴结构，而非复制句子。
- 检查正文是否真正覆盖“谁负责、怎么接、如何复用、如何校验”。
- 检查 Topic / Draft / Cover / Review 的 archive timestamp 是否统一到 `20260405-174656`。
- 检查输出是否只用于预览、复审与手动发布，不自动公开发布。

8. 风险边界
- 不碰 `master/main`。
- 不动 `node5`。
- 不做公开发布。
- 不编造趋势证据。
- 不把 fallback 当成已经成功执行。
- 不把未实际获取的页面内容包装成已完成抓取。

9. 归档要求
- 最终内容包必须能按 `date / timestamp+中文标题 / material files` 组织。
- `date` 层：`2026-04-05`。
- `timestamp+中文标题` 层：`20260405-174656+AI技能开始卷稳定交付链了真正拉开差距的是谁能把Skill接进可复用workflow`。
- `material files` 层建议保留：
  - `Boss/BOSS-CYCLE-20260405-174656.md`
  - `.role-sessions/Topic/@TOPIC`
  - `Draft/@DRAFT.md`
  - `Cover/@COVER`
  - `Review/@REVIEW`
  - 来源状态说明 / 选题依据摘要 / 正文依据摘要 / 封面方向说明 / 复审检查记录

10. 交付说明
- 本轮 Boss 只写一个 boss task file。
- 下游角色以本文件为准继续产出，并统一对齐本轮 archive package。
- `SAF-TaskAssignAndProgress.md` 本轮未在仓库中检出，不额外引用不存在文件。
- 本文件已满足 archive-ready 交付要求，可直接作为本轮 Boss 唯一任务包归档。
